import { CricketIPLPage } from "@/sports/cricket/pages/CricketIPLPage";

export const metadata = {
  title: "IPL — SportSphere",
  description: "Indian Premier League — matches, points table, franchises and records.",
};

export default function IPLRoute() {
  return <CricketIPLPage />;
}
